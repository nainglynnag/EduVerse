<div class="p-6 bg-white rounded-lg">
  <h3 class="text-xl font-bold mb-4">Update Student Information</h3>
  <form action="/admin/edit-student/<%= form.id %>" method="POST">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2"
          >Full Name *</label
        >
        <input
          type="text"
          name="name"
          required
          value="<%= typeof form !== 'undefined' && form.name ? form.name : '' %>"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2"
          >Student ID</label
        >
        <input
          type="text"
          name="student_code"
          disabled
          value="<%= typeof form !== 'undefined' && form.student_code ? form.student_code : '' %>"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2"
          >Email *</label
        >
        <input
          type="email"
          name="email"
          required
          pattern="^[^\s@]+@[^\s@]+\.[^\s@]+$"
          value="<%= typeof form !== 'undefined' && form.email ? form.email : '' %>"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-700 mb-2"
          >Password *</label
        >
        <input
          type="password"
          name="password"
          value="<%= typeof form !== 'undefined' && form.password ? form.password : '' %>"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        />
      </div>

      <div>
        <label class="block text-sm mb-1">Current Plan</label>
        <select
          name="plan"
          required
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        >
          <option value="">Current Plan</option>
          <% studentPlans.map((plan) => { %>
          <option value="<%= plan.id %>" <%= (typeof form !== 'undefined' && (form.plan.toLowerCase() === plan.name.toLowerCase() || Number(form.plan) === plan.id)) ? 'selected' : '' %>><%= plan.name %></option>
        <% }) %>
        </select>
      </div>

      <div>
        <label class="block text-sm mb-1">Status</label>
        <select
          name="status"
          required
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        >
          <option value="">Status</option>
          <% statuses.map((status) => { %><option value="<%= status %>" <%= (typeof form !== 'undefined' && form.status.toLowerCase() === status.toLowerCase()) ? 'selected' : '' %>><%= status %></option><% }) %>
        </select>
      </div>

      <div class="md:col-span-2">
        <label class="block text-sm font-medium text-gray-700 mb-2"
          >Plan Description</label
        >
        <textarea
          name="plan_description"
          rows="2"
          disabled
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-transparent"
        >
<%= typeof form !== 'undefined' && form.plan_description ? form.plan_description.trim() : 'N/A' %></textarea
        >
      </div>
    </div>

    <% if (typeof error !=='undefined' && error) { %>
    <div id="flashError" class="mb-4 text-sm text-red-600"><%= error %></div>
    <% } %>

    <div class="mt-4 flex justify-end space-x-2">
      <button
        type="button"
        onclick="window.history.back()"
        class="px-6 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 active:scale-90 transition-all duration-300"
      >
        Cancel
      </button>
      <button
        type="submit"
        class="px-6 py-2 bg-gradient-to-r from-red-600 to-orange-600 text-white rounded-lg hover:from-red-700 hover:to-orange-700 active:scale-90 transition-all duration-300"
      >
        Save
      </button>
    </div>
  </form>
</div>

<script>
  setTimeout(() => {
    const el = document.getElementById("flashError");
    if (el) el.remove();
  }, 3000);
</script>
